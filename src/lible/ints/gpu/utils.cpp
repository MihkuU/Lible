#include <lible/ints/gpu/utils.hpp>

// namespace LIG = lible::ints::gpu;

// namespace lible::ints::gpu
// {
    // extern "C" 
    // __device__  int LIG::idxE(int d3, int d23, int i, int j, int t, int pos)
    // {
    //     return pos + i * d23 + j * d3 + t;
    // }

    // // extern "C"
    //  __device__ int LIG::dimCartIdxs(int l)
    // {
    //     return (l + 1) * (l + 1) / 2;
    // }
// }